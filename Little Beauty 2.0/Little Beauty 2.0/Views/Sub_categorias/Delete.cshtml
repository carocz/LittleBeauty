@model Little_Beauty_2._0.Models.Sub_categorias

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_LayoutCompras.cshtml";
}
<div class="container body-content">
    <h2>Delete</h2>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Sub_categorias</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.nombre_sub_categoria)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.nombre_sub_categoria)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Categorias.nombre_categoria)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Categorias.nombre_categoria)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
</div>